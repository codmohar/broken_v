# ML Models

Store backend inference models here.

Current expected model:

```text
plant_disease_model.keras
```

For retraining work, use the separate workspace:

```text
plant_disease_training/
```

